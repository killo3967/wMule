# Man pages are not built on Windows
# The original check_manpage() macro from autotools is not available in CMake
# Man pages can be generated from sources if needed

# If you need man pages on Windows, consider using pandoc or similar tools
# to convert documentation from other formats
